import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { ThemeProvider } from '@/components/theme-provider'
import { Toaster } from '@/components/ui/toaster'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Avilink AI Agent - Advanced AI Assistant',
  description: 'A powerful AI assistant with DeepSearch, code execution, web browsing, file processing, and multi-LLM support. Built with Next.js and TypeScript.',
  keywords: ['AI', 'Assistant', 'DeepSearch', 'Code Execution', 'Multi-LLM', 'Next.js'],
  authors: [{ name: 'Avilink Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  )
}